package com.github.bkmbigo.passionfruitdetector.domain.models.settings

enum class PassionFruitModel(val title: String, val description: String) {
    EFFICIENT_DET_0("EfficientDet0", "A fast object detection model with a relatively lower accuracy but fast inference times")
}
